import { Resend } from 'resend';
import { baseUrl,fail,noStore,parse,sql,token,tokenHash,validEmail } from '../_auth.js';
export default async function handler(req,res){noStore(res);if(req.method!=='POST'){res.setHeader('Allow','POST');fail(res,405,'Method not allowed.');return;}if(!process.env.RESEND_API_KEY||!process.env.LINK_FROM_EMAIL){fail(res,503,'Email sign-in is temporarily unavailable.');return;}try{const body=parse(req),email=String(body.email||'').trim().toLowerCase();if(!validEmail(email)){fail(res,400,'Enter a valid email address.');return;}const db=sql();const rows=await db`
SELECT c.id AS contact_id,c.organization_id,c.full_name,o.display_name
FROM organization_contacts c JOIN organizations o ON o.id=c.organization_id
WHERE lower(c.email)=lower(${email}) AND o.active=true AND o.approval_status='approved' AND o.service_area_verified=true
ORDER BY c.is_primary DESC,c.created_at LIMIT 1`;
if(!rows.length){res.status(200).json({ok:true,message:'If this email is authorized for an approved LINK nonprofit, a sign-in link is on the way.'});return;}const r=rows[0];const recent=await db`SELECT id FROM hub_nonprofit_portal_magic_tokens WHERE contact_id=${r.contact_id} AND used_at IS NULL AND expires_at>now() AND created_at>now()-interval '60 seconds' LIMIT 1`;if(recent.length){res.status(200).json({ok:true,message:'A sign-in link was recently sent. Please check your email.'});return;}const raw=token();await db`INSERT INTO hub_nonprofit_portal_magic_tokens (organization_id,contact_id,email,token_hash,expires_at) VALUES (${r.organization_id},${r.contact_id},${email},${tokenHash(raw)},now()+interval '20 minutes')`;const link=`${baseUrl(req)}/api/nonprofits/portal/auth/verify?token=${encodeURIComponent(raw)}`;const resend=new Resend(process.env.RESEND_API_KEY);const {error}=await resend.emails.send({from:process.env.LINK_FROM_EMAIL,to:email,subject:'Your LINK Nonprofit Portal sign-in link',html:`<div style="font-family:Arial,sans-serif;color:#0b2344;line-height:1.55"><h2>LINK Community Hub™</h2><p>Use the secure link below to sign in to the nonprofit portal for <strong>${String(r.display_name).replace(/[<>&]/g,'')}</strong>.</p><p><a href="${link}">Sign in to LINK</a></p><p>This link expires in 20 minutes.</p></div>`});if(error)throw new Error(error.message||'Email delivery failed.');res.status(200).json({ok:true,message:'Check your email for your secure LINK sign-in link.'});}catch(e){console.error('LINK nonprofit auth request error:',e);fail(res,500,'We could not send your sign-in link.');}}
